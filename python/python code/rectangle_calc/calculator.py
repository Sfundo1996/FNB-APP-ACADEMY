def area(length , width):
    #function to calculate the area
    return length * width

def parimeter(length , width):
    #function to calculate the perimeter
    return (length + width ) * 2
